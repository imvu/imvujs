/*global IMVU:true, TEST_MAX_OUTPUT_SIZE*/
module({
    assert: 'assert.js'
}, function (imports) {
    var g = 'undefined' === typeof window ? global : window;

    function asyncTest(name, fn) {
        if (arguments.length !== 2) {
            throw new TypeError('asyncTest requires 2 arguments');
        }
    }
});
